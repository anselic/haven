// Walking into a namespace that exists but asking for a name it doesn't export.
//@ error: is not exported by module 'dsp::osc'

import std/dsp

@export
proc main() i32 {
    return dsp::osc::no_such_thing();
}
