// Walking into a namespace that exists but asking for a name it doesn't export.
//@ error: is not exported by module 'clap::version'
//@ dep: dsp=stdlib/dsp
//@ dep: plug=stdlib/plug

import plug/clap

@export
proc main() i32 {
    return clap::version::no_such_thing();
}
