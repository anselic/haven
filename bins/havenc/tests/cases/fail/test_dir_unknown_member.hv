// Walking into a namespace that exists but asking for a name it doesn't export.
//@ error: is not exported by module 'shapes::rect'
//@ dep: geom=bins/havenc/tests/pkgs/geom

import geom/shapes

@export
proc main() i32 {
    return shapes::rect::no_such_thing();
}
