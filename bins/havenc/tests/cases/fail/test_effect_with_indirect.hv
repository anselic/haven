//@ error: has effect clause `with [Alloc]` but calls code with unknown effects
// A function pointer has no effect bound in its type yet.
proc id(x: i32) i32 { return x; }

proc apply(f: proc(i32) i32, x: i32) i32 with [Alloc] {
    return f(x);
}

@export
proc main() i32 { return apply(id, 2); }
