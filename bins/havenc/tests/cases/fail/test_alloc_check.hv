//@ error: has effect clause `without [Alloc]`
// println comes from the prelude; an extern without a no-allocation clause is
// treated as dirty (may allocate), so anything calling it becomes dirty too
proc dirty() { println(0); }

@export
proc main() without [Alloc] {
    dirty();
    dirty2();
}

proc dirty2() { dirty(); }
