// A code-generation hint for functions has no meaning on a struct.
//@ error: cannot be written on a struct

@inline(always)
struct Point { x: i32, y: i32 }

@export
proc main() i32 {
    return 0;
}
